<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="HelloMath" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Themes. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="HelloMath.PopClose">

        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>

    </style>

    <style name="SpinnerStyle" parent="@android:style/Widget.TextView.SpinnerItem">
        <item name="android:textSize">22sp</item>
        <item name="android:background">#787878</item>
    </style>

    <style name="HelloMath.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="HelloMath.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
